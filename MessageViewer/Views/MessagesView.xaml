<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:TextMessageViewerWPF.ViewModels"
             xmlns:views="clr-namespace:TextMessageViewerWPF.Views"
             x:Class="TextMessageViewerWPF.Views.TextMessagesView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="DataTemplate1">
            <StackPanel>
                <TextBlock Text="{Binding}" />
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <UserControl.DataContext>
        <viewModels:TextMessagesViewModel/>
    </UserControl.DataContext>
    <Grid>
    	<Grid.RowDefinitions>
    		<RowDefinition Height="*"/>
    		<RowDefinition Height="Auto"/>
    	</Grid.RowDefinitions>

        <ListView Margin="5" ItemsSource="{Binding TextMessages}" IsSynchronizedWithCurrentItem="True" SelectedItem="{Binding Path=CurrentTextMessage, Mode=TwoWay}" >
            
            <ListView.View>
        		<GridView>
                    <GridViewColumn Header="From" DisplayMemberBinding="{Binding Path=TextMessage.From}"/>
                    <GridViewColumn Header="Message" DisplayMemberBinding="{Binding Path=TextMessage.MessageText}"/>
                    <GridViewColumn Header="To" DisplayMemberBinding="{Binding Path=TextMessage.To}"/>
                    <GridViewColumn Header="Date and Time" DisplayMemberBinding="{Binding Path=TextMessage.DateTime}"/>
        		</GridView>
        	</ListView.View>
        </ListView>
        <views:TextMessageView Margin="5" Grid.Row="1" DataContext="{Binding Path=CurrentTextMessage}"/>

    </Grid>
</UserControl>
